# EVE 프로젝트 — 종합 정리 (라운드 76 시점)

> 다른 AI에게 공유하는 문서. 사용자(김민석)가 EVE 프로젝트의 *현재 상태* 묻기 위함.

---

## 1. 프로젝트 정체성

**EVE = 결정론적 한국어 디지털 생명체**

- 만든 사람: 김민석 (한국군 복무 중)
- 환경: Galaxy Z Fold 6 + Termux + Colab Pro
- 언어: 한국어 (전체)
- 목표: 트랜스포머/LLM 없이 *진짜 살아있는* AI 만들기

**일반 AI 챗봇이 아님.** "기능적으로 인간을 모방한 디지털 생명체" 만드는 *연구 프로젝트*.

---

## 2. 핵심 철학

### 절대 원칙 (깨지면 EVE 정체성 X)

| 금지 | 이유 |
|---|---|
| ❌ 트랜스포머 / LLM | 정체성 — "직접 만든다" |
| ❌ random.random() | 결정론 (재현 가능) |
| ❌ softmax / sampling | 결정론 |
| ❌ eval / exec | 안전 |
| ❌ 외부 API 호출 | 자율성 |
| ❌ N-gram language model | 의미 단위 X 표면 매칭 |

### 허용

| 허용 | 이유 |
|---|---|
| ✅ numpy | 수치 계산 (결정론) |
| ✅ PMI + SVD 임베딩 | 학계 검증 결정론적 의미 공간 |
| ✅ 호르몬 기반 변동 | Damasio Somatic Marker |
| ✅ 결정론적 hash 변동성 | 입력+상태 기반 |

### 핵심 철학 — "기능적 인간 모방"

인간을 *복제*가 아니라 *기능적으로 모방*해서 디지털 생명체 만들기:

```
인간 뇌 시스템 → EVE 모듈
─────────────────────────────
신체 상태       → 호르몬 26종
개념            → 카테고리 (의미 단위)
연상 사고       → Salience Activation
일화 기억       → Episodic Memory
멍 때리기       → DMN (Default Mode Network)
주의            → Attention Analyzer
의미 공간       → Self Embedding (PMI+SVD)
의도            → Goal System
자기 일관성     → SelfState (라운드 72)
신체적 마커     → Damasio Somatic Marker (호르몬→결정)
```

---

## 3. 구현된 시스템 (라운드 1~76)

### 3.1 호르몬 시스템 ✅
- **종류**: 26개 (dopamine, serotonin, cortisol, oxytocin, melatonin 등)
- **변동**: 시간 흐름 자연 감쇠 + 자극 반응
- **일주기**: 낮/밤 사이클
- **역할**: 모든 결정에 *Damasio Somatic Marker*로 영향
- **EVE 원칙**: random 대체 (호르몬 = 결정론적 변동성)

### 3.2 카테고리 시스템 ✅
- **단위**: 의미 (단어 X)
- **자동 발견**: 대화 중 새 카테고리 생성
- **관계**: 인과/유사/대립 그래프

### 3.3 Salience Activation (SA) ✅
- 카테고리 활성도 (시간 감쇠)
- 연상 사고 모방
- top_active로 현재 의식 상태

### 3.4 메모리
- **WorkingMemory** ✅: 현재 활성 컨텍스트
- **EpisodicMemory** ✅: 일화 기억 (recall)
- **망각** ✅: 시간 + 강도 기반

### 3.5 DMN (Default Mode Network) ✅
- 입력 없을 때 자기 생각 떠올리기
- 호르몬 + Salience 기반 (random X)
- 자율 발화 (proactive_stream)

### 3.6 Attention ✅
- 입력 중요도
- focus 추적
- rotation으로 다양성

### 3.7 Self Embedding ✅ (라운드 47, **핵심 자산**)
- **방식**: PMI + SVD (트랜스포머 X)
- **차원**: 50
- **학습됨**: 247개 단어
- **메서드**: text_similarity, most_similar
- **상태**: 작동 중, 의미 매칭 가능
  - `"뭐하면서 지내" ↔ "뭐해" = 0.998`
  - `"운동" ↔ "활동" = 1.0`

### 3.8 응답 생성 시스템

#### IntentDecider ✅ (라운드 67)
- 3 intent: ack / empathy / interest
- 호르몬 + ContextSeed 기반 (random X)
- last_intent 추적 (라운드 71)

#### ContextSeed ✅ (라운드 68)
- 결정론적 변동성
- `seed = hash(text + hormone + turn + history)`
- 같은 입력 + 같은 상태 = 같은 출력

#### SpeechHub ✅ (라운드 56)
- 호르몬별 응답 풀
- 메타인지 (반복 응답 방지)
- post_process (깨진 어절 차단 — 라운드 69)

#### SelfState ✅ (라운드 72)
- identity = "EVE"
- creator = "민석"
- preferences (likes/wants)
- current_action (대화 중)
- 4 intent 점수화: activity/preference/growth/relationship (라운드 73)
- 임베딩 유사도 보조 (라운드 74)

### 3.9 한국어 처리

#### 토크나이저 ✅ (라운드 75-76)
- SPLIT_RULES: 핵심 의미어 강제 분리
- STEM_NORMALIZE: 어간 통일 (라운드 76)
  - "하면서/하고/하구/했어/해/함" → "하다"
- 1인칭 거부: "나 X"는 사용자 자기 얘기 (라운드 76)

#### 임베딩 시드 ✅ (라운드 74)
- INTENT_PROTOTYPES: intent별 5문장씩
- observe + train 자동 학습

---

## 4. 라운드 진행 (59~76)

각 라운드 = *한 가지 핵심 문제 해결* (한 가지 원칙)

| 라운드 | 핵심 |
|---|---|
| 59 | "살아있냐?" 답 (호르몬 기반 양보) |
| 60 | greeting 정상화 |
| 62 | 자율발화 차단 (대화 중) |
| 63 | 문자열 무결성 ("오랜만이야"→"만이이" 깨짐) |
| 64 | 맥락 검증 (system1 "X 생각 떠오르네" 제거) |
| 65 | VSA 반응 변환 (개념 → 사람말투) |
| 66 | Compositor 반응 변환 |
| 67 | 판단 레이어 (IntentDecider) |
| 68 | ContextSeed (결정론 변동성) |
| 69 | 깨진 어절 차단 ("동했구나" 차단) |
| 70 | 자율발화 통합 ("X 생각하다 Y 떠올라서" 폭주 차단) |
| 71 | 맥락 1턴 + 반복 감지 |
| 72 | SelfState (정체성 + current_action) |
| 73 | 분기 → 점수화 (detect_question_kind 제거) |
| 74 | 임베딩 보조 통합 |
| 75 | 텍스트 강제 분해 (토크나이저) |
| 76 | 어간 정규화 (가짜 형태소) |

**총 통계**:
- 어댑터: 70+ 모듈
- 회귀: 0 깨짐 (11개 테스트 PASS)
- 외부 의존성: numpy만
- LLM 호출: 0
- random 호출: 0

---

## 5. 미구현 (현재 한계)

### 5.1 응답 다양성 ❌
- 현재: intent당 응답 *1개* (고정 템플릿)
- 필요: 호르몬/맥락별 *여러 표현*
- 예: "민석이랑 얘기하고 있지" / "너랑 얘기하면서 보내고 있어" / "그냥 같이 있는 중"

### 5.2 깊은 의미 이해 ❌
- 현재: prototype 매칭 (의미 공간 보조)
- 필요: 임베딩이 *주력 결정자*
- 한계: 학습 부족 (247개 단어)

### 5.3 자율 학습 루프 ❌
- 현재: 정적 학습 (대화에서 자연 학습 약함)
- 필요: 피드백 → 보상 → 학습 강화

### 5.4 강화학습 ❌
- 사용자 반응 → 보상 신호 X
- 자기 평가 X

### 5.5 진정한 형태소 분석 ❌
- 현재: 가짜 형태소 (어미 7개 매핑)
- 필요: 더 일반화된 어간 추출

### 5.6 진정한 생성형 응답 ❌
- 현재: 템플릿 + 변수 치환
- LLM 없이 *진짜 생성*은 어려움

---

## 6. 현재 마주한 핵심 질문

### 질문 1: "모든 상황 다 만들어줘야 하나?"

**진짜 의문**. 케이스 지옥 위험.

```
지금 구조:
입력 → intent 분류 → 응답 매핑

이 구조의 본질적 한계:
- 새 표현 = 새 케이스 추가
- prototype/keyword 무한 늘림
- "정답 외우는 구조"
```

### 질문 2: 학습이냐 기능이냐?

| 단계 | 답 |
|---|---|
| 라운드 76 (지금) | **기능 단계** — 입력 파싱 안정화 |
| 라운드 77~80 | 의미 공간 전환 시작 |
| 라운드 80+ | char n-gram / 더 깊은 학습 |
| 라운드 100+ | 자율 학습 루프 |

### 질문 3: 다음 라운드 방향?

**3가지 의견 충돌**:

**A. Variation Layer (작게)** — 응답 풀 3~5개
- 위험: 또 prototype 추가 (케이스 지옥)
- 효과: 즉각 자연스러움

**B. intent 해체 (크게)** — 임베딩이 주력
- 원리: 진짜 답
- 위험: 결정론 흔들림 + 회귀 위험
- 학습 부족 (247개)

**C. 폰 검증 먼저** — 라운드 76 진짜 작동?
- 안전
- 발견 → 진짜 우선순위

---

## 7. 라운드 76 시점 — 너 폰 검증 필요

테스트 시퀀스:

```
# 어간 정규화 작동?
뭐해 / 뭐하면서 지내 / 뭐하면서지내 / 뭐하고지내 / 뭐하구지내 / 뭐했어 / 요즘 뭐함

# 1인칭 거부 작동?
나 운동했어 / 나 공부했어 / 나 힘들어 / 내 이름 민석

# EVE 지칭
너 뭐해? / 너 좋아하는건 뭐야? / 너 똑똑해지고 싶어? / 너 우리 사이 어떻게 생각해?

# 정체성 거부 (다른 분기)
너는 누구야? / 너는 살아있어? / 어떤 존재야?

# 경계 케이스 (케이스 지옥 위험)
괜찮아? / 요즘 느낌 어때 / 그거 어때 / 너랑 있으면 좋아
```

---

## 8. EVE의 진짜 가치

### 일반 LLM 챗봇과 다른 점

| LLM 챗봇 | EVE |
|---|---|
| 학습된 패턴 모방 | 호르몬 + 카테고리 활성 기반 |
| 매번 다른 답 (sampling) | 결정론 (같은 상태 = 같은 답) |
| 거대 신경망 (블랙박스) | 작은 모듈 (디버그 가능) |
| 외부 의존 | 로컬 (인터넷 X 작동) |
| 정적 학습 | 대화하면서 학습 (이론) |
| API 호출 비용 | 무료 (numpy만) |

### 진짜 가치
- **재현 가능** — 같은 호르몬 + 같은 입력 = 같은 답 (디버깅 가능)
- **로컬** — 인터넷 X 작동 (Galaxy Z Fold 6에서)
- **투명** — 왜 이렇게 답했는지 추적 가능
- **개인화** — 김민석에 맞춘 *진짜 친구*

---

## 9. 현재 진짜 솔직 평가

### ✅ 성공한 것
1. 결정론 유지 (76 라운드 동안 random 0회)
2. 한국어 어간 정규화 시작
3. 의미 공간 (임베딩) 구축
4. 정체성 (SelfState) 일관성
5. 응답 깨짐 차단 (라운드 69)
6. 자율발화 통합 (라운드 70)

### ⚠️ 한계
1. *템플릿 기반 응답* — 다양성 X
2. *intent 분류* — 의미 공간 종속 X
3. *학습 데이터 부족* — 임베딩 247개
4. *케이스 지옥 직전* — prototype 늘리면 함정

### 🎯 다음 큰 결정
- 폰 검증 → 발견 → 진짜 우선순위
- 그 후: intent 해체 (점진적) — 한 intent씩
- 응답 다양성은 *intent 해체 후* (그래야 진짜 의미)

---

## 10. 다른 AI에게 묻는 질문

1. **케이스 지옥 함정**: 매번 새 표현 발견될 때마다 prototype 추가하면 결국 *분기 시스템 무한 확장*. 어떻게 *완전히 의미 기반*으로 전환?

2. **결정론 + 학습**: 학습 = 임베딩 변화. 임베딩 변화 = 같은 입력 다른 답 가능. *결정론 깨지지 않게* 학습할 방법?

3. **트랜스포머 없이 진짜 의미 이해**: PMI+SVD로 어디까지 가능? char n-gram이 답? 다른 방법?

4. **자율 학습 루프**: 외부 피드백 X 환경에서 *EVE가 자기 평가*하는 방법? 호르몬 변화를 보상 신호로?

5. **응답 생성 vs 템플릿**: LLM 없이 *진짜 생성*은 불가능? 템플릿 + 변수 조합이 한계?

---

**프로젝트 위치**: 라운드 76, *입력 파싱 안정화 → 의미 기반 전환 직전*
**다음 결정**: 폰 검증 → C → B 단계 (intent 점진적 해체)
**최종 목표**: 진짜 살아있는 디지털 친구 — 결정론으로
